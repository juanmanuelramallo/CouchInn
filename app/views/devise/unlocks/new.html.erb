<body class="unlock-user">

<div class="col-md-5 col-centered">

<h2>Reenviar instrucciones de desbloqueo</h2>

<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>


  <% if !devise_error_messages!.blank? %>
    <div class="alert alert-danger">
    <%= devise_error_messages! %>
    </div>
  <% end %>


  <%= f.label :email %>
  <div class="input-group">
    <span class="input-group-addon" id="basic-addon1">📧</span>
    <%= f.email_field :email, autofocus: true, class:"form-control" %>
  </div>

  <div class="spacer-min"></div>

  <div class="actions">
    <%= f.submit "Reenviar instrucciones de desbloqueo", class:"btn btn-success btn-block" %>
  </div>
<% end %>

<div class="spacer-div"></div>

<div class="">
  <h5> Si estás acá por error, sabés que siempre podés: </h5>
</div>

<%= render "devise/shared/links" %>

</div>

</body>
