
<div class="col-md-5 col-centered">

<h2>Change your password</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>


  <% if !devise_error_messages!.blank? %>
    <div class="alert alert-danger">
    <%= devise_error_messages! %>
    </div>
  <% end %>


  <%= f.hidden_field :reset_password_token %>

   <%= f.label :password, "Nueva contraseña", class:"label label-default" %><br />
    <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em><br />
    <% end %>
  <div class="input-group">
    <span class="input-group-addon" id="basic-addon1">🔑</span>
    <%= f.password_field :password, autofocus: true, autocomplete: "off", class:"form-control" %>
  </div>

  <div class="spacer-div"></div>

  <%= f.label :password_confirmation, "Confirmar nueva contraseña", class:"label label-default" %><br />
  <div class="spacer-min"></div>
  <div class="input-group">
    <span class="input-group-addon" id="basic-addon1">🔑</span>
    <%= f.password_field :password_confirmation, autocomplete: "off", class:"form-control" %>
  </div>

  <div class="spacer-div"></div>

  <div class="actions">
    <%= f.submit "Cambiar mi contraseña", class:"btn btn-success btn-block" %>
  </div>
<% end %>

<div class="spacer-div"> </div>

<div class="">
  <h5> Si estás acá por error, sabés que siempre podés: </h5>
</div>

<%= render "devise/shared/links" %>

</div>
